# PKG-013 — P22: ustawienia, remapowanie klawiszy i dostępność (bramka B / MVP)

Katalog: `C:\retro-ski-jumping` — gra przeglądarkowa TypeScript + Vite +
Canvas2D, sterowana klawiaturą, styl DOS pixel-art (**nie Godot**).
Pakiet: **PKG-013**. Zakres: **P22** z `docs/IMPLEMENTATION_PLAN.md` —
ustawienia, pomoc wejścia i dostępność; po nim ocena **bramki B / MVP**.
Nie zaczynaj P23+, H05–H20 ani nowych trybów.

## Lektura (tylko potrzebna)

1. `AGENTS.md` (zakaz Git, prostota, jedno review po pakiecie, zamrożenie
   zawartości, obowiązkowe przekazanie), `docs/README.md`, ten prompt,
   `docs/PACKAGE_WORKFLOW.md` §§3–5.
2. `docs/IMPLEMENTATION_PLAN.md` — „P22” i „Bramka B — MVP”.
3. `docs/GAMEPLAY_SPEC.md` §1 (akcje, klawisze, zasady wejścia),
   `docs/ART_UI_AUDIO.md` §7–8 (UI gry DOS, dostępność i czytelność),
   `docs/TECHNICAL_DESIGN.md` §4 (pętla i wejście) i §6 (zapis, magazyn
   `settings`), odpowiednie wpisy `docs/QA_ACCEPTANCE.md`.
4. Router `.agents/skills/retro-ski-support/SKILL.md`; z niego tylko
   potrzebne skille (np. typografia bitmapowa przy dużym tekście, QA gry
   przy Playwright). Nie skanuj całej biblioteki; starych raportów nie czytaj.

**Trwały zakaz wszystkich poleceń Git** (bez repozytorium, stage, commit,
reset itd.). Zapisuj bezpośrednio na dysku. Backup
`retro-ski-jumping-23-09-2026-backup` zostaw nietknięty.

## Stan wejściowy (24.09.2026)

- PKG-001–012 COMPLETE, **P21 COMPLETE**: grywalne technical K120/HS134,
  H01 Lillehammer K90/HS98, H02 Zakopane K125/HS140, H03 Oberstdorf
  K120/HS137, H04 Planica K200/HS240 (`h04-inspired-4`, fizyka
  `pkg008-tune-9+h04-polar-1`); każda z VISUAL USER PASS. Trening, konkurs,
  AI, hotseat, punktacja, zapis (IndexedDB) i replay działają.
- Wejście: stała mapa `ACTION_BY_CODE` w `src/input/keyboard.ts`
  (↑ wybicie, ←/→ pochylenie, T/R lądowanie); obsługa menu i skrótów
  globalnych w `src/app/main.ts` (`onKeyDown`). **Brak** ekranu ustawień,
  zapisu ustawień i remapowania. Magazyny IndexedDB (`src/storage/schema.ts`
  `STORE`): sessions, results, records, replays, leases — bez `settings`.
- Ostatnia bramka: `npm test` 36 plików/316 PASS, Playwright H04 3/3 PASS.
  Zewnętrzny PLAYABILITY NOT RUN.

## Wynik pakietu (P22)

1. Ekran USTAWIENIA dostępny z menu klawiaturą, w stylu istniejącego UI
   (bitmapowy font, siatka pikseli, bez tekstu wektorowego przeglądarki).
2. Remapowanie akcji skoku (wybicie, lewo, prawo, telemark, dwie nogi) oraz
   przynajmniej potwierdzenia/powrotu w menu: przechwycenie klawisza,
   wykrycie i rozwiązanie konfliktu, przywrócenie domyślnych. Nawigacja
   ratunkowa (Enter/Backspace/strzałki w menu i pomoc) nie może zostać
   trwale odcięta.
3. Głośność (jedna główna wystarczy, jeśli brak osobnych kanałów), skala
   obrazu zgodnie z polityką z `ART_UI_AUDIO` („Dopasuj”/„Równe piksele”,
   jeśli już istnieje — nie dubluj algorytmu), duży tekst tabel, ograniczenie
   pogody/ruchu tła.
4. Trwały zapis ustawień (najprościej: nowy magazyn `settings` w IndexedDB
   z migracją wersji bazy, albo uzasadniona prostsza ścieżka) z walidacją
   i bezpiecznym powrotem do domyślnych przy uszkodzonych danych.
5. Bindy obowiązują wszędzie, gdzie gra czyta akcje (trening, konkurs,
   hotseat); replay odtwarza zapisane próbki niezależnie od aktualnych bindów.
6. Znaki PL/IME: jeśli istnieje pole tekstowe (np. nazwa gracza), litery
   w nim nie wykonują akcji gry; jeśli pola nie ma, nie twórz go na zapas.

## Kolejność

Najpierw model ustawień + zapis i test jednostkowy, potem mapowanie wejścia
(zastąpienie stałej mapy bieżącymi bindami, bez zmiany semantyki pressed/
held/auto-repeat), potem ekran ustawień, na końcu dostępność (duży tekst,
ograniczenie ruchu). Po całym zakresie: jedno końcowe review.

## Kryteria odbioru

- Pełna ścieżka klawiaturą: menu → ustawienia → remap → skok z nowymi
  bindami → reload strony → bindy zachowane → przywrócenie domyślnych.
- Konflikt bindów obsłużony czytelnie; nawigacja ratunkowa zawsze działa.
- Replay nagrany przed zmianą bindów odtwarza się identycznie.
- Brak regresji fizyki/punktacji: deterministyczne testy skoczni bez zmian
  fixture; wersje fizyki/skoczni bez zmian (P22 nie zmienia wyników).
- `npm run typecheck`, `npm test`, `npm run build`; nowy spec Playwright
  ustawień oraz regresja `tests/browser/shell.spec.ts` i jednej skoczni
  (np. `h04.spec.ts`) z unikalnym `--output=docs/evidence/PKG-013/tmp-…`
  (najpierw `Test-Path`, nic nie nadpisuj).
- Zrzuty ekranu ustawień do odbioru; **VISUAL wydaje wyłącznie użytkownik**.
  Bramkę B / MVP oceń w raporcie faktami; nie wpisuj PASS za użytkownika.

## Prostota, review, przekazanie

Najprostsze rozwiązanie dające dobry efekt: bez frameworka UI, event busa czy
warstw adapterów. Testy w trakcie to nie review; **jedno końcowe review** po
całym P22, potem celowane poprawki i sprawdzenie zmienionych ścieżek.
Wznowienie po przerwaniu: sprawdź stan dysku i raport, kontynuuj brakujące
kroki. Zapisz krótki `docs/evidence/PKG-013/REPORT.md` (statusy, polecenia
i wyniki, dowody, „Końcowe review”, ograniczenia). Po zamknięciu napisz prompt
**PKG-014 (P23, P24, P25 — sezon, własny kalendarz, silnik KO)** w
`docs/handoffs/PKG-014.md` i bajtowo identycznie w
`docs/NEXT_SESSION_PROMPT.md`; zaktualizuj statusy w `docs/README.md`,
`docs/PACKAGE_WORKFLOW.md`, `docs/IMPLEMENTATION_PLAN.md`. Przy braku odbioru
użytkownika lub niespełnionych kryteriach: PKG-013 INCOMPLETE i kontynuacja
tego samego pakietu (poprzedni prompt archiwizuj jako `PKG-013-CONTINUE-01.md`).
Nie publikuj projektu i nie wysyłaj wiadomości do osób trzecich bez polecenia.
